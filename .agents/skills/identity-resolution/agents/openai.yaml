interface:
  display_name: "Identity Resolution"
  short_description: "Review aliases and duplicate identities"
  default_prompt: "Use $identity-resolution to review aliases, duplicate entities, and ambiguous public-record identities in a CRK case without auto-merging evidence."
