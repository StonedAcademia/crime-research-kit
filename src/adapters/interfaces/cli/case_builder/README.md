# Case Builder CLI

Typer app and handler bridge for the `cr-kit` console script. The public shim
remains `src/cli.py`; implementation lives here so wrapper and handler modules
stay under the repository size limit.
