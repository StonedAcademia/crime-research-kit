# parsing

Local document parsing and OCR wrappers. These modules may update a source's
`text_path` when a local parse artifact is created, but extracted facts still
must go through staged extraction and validation.
