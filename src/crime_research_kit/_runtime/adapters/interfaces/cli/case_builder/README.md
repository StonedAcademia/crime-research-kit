# Case Builder CLI

Typer app and handler bridge for the `cr-kit` console script. The console
entrypoint lives at `crime_research_kit._runtime.cli`; implementation lives here
so wrapper and handler modules stay under the repository size limit.
