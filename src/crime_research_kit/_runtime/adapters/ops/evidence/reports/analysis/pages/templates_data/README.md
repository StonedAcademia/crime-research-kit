# Report Template Data

This package-data directory holds Jinja2 report templates and committed static
assets for the typed report rendering layer.

`static/app.css` and `static/app.js` are generated from `frontend/` and checked
in so installed packages can render reports without a Node toolchain at runtime.
