# SDK Examples

Importable SDK recipes for documentation and smoke tests.

These examples show common public SDK calls without creating another runtime
layer. They return `OperationResult` objects and use dry-run contexts for
operations that would otherwise fetch sources, write exports, or resume
workflows.

