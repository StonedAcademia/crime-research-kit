# Bootstrap configuration helpers

Stdlib-only support code for `deployment/scripts/bootstrap_env.py`. The wrapper
stays as the public entrypoint while this directory keeps validation, rendering,
and CLI flow small enough for repository-shape governance.
