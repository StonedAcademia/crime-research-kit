use_default_settings: true

general:
  debug: false
  instance_name: "TRCR SearXNG"

search:
  safe_search: 0
  autocomplete: ""
  default_lang: "en"

server:
  bind_address: "0.0.0.0"
  port: 8080
  secret_key: "change-me-before-sharing"
  limiter: true
  image_proxy: false
  method: "GET"

valkey:
  url: redis://searxng-valkey:6379/0

ui:
  static_use_hash: true

outgoing:
  request_timeout: 8.0
