name: Audit

on:
  workflow_dispatch:
  schedule:
    - cron: "17 9 * * 1"
  push:
    paths:
      - pyproject.toml
      - deployment/tooling/manifest.json
      - deployment/scripts/tools/fetch_governance_tools.py
      - deployment/scripts/checks/license_policy.py
      - .gitleaks.toml
  pull_request:
    paths:
      - pyproject.toml
      - deployment/tooling/manifest.json
      - deployment/scripts/tools/fetch_governance_tools.py
      - deployment/scripts/checks/license_policy.py
      - .gitleaks.toml

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - uses: moonrepo/setup-toolchain@v0
      - run: make install-governance
      - run: make audit-secrets audit-deps audit-licenses
      - run: make audit-links
        continue-on-error: true
