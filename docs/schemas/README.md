# Schema Groups

Schemas are grouped by the ledger domain they validate:

- `case/` covers source, entity, place, and artifact records.
- `evidence/` covers claims, events, event links, and relationships.
- `review/` covers quotes, source spans, research actions, and redactions.

Lane registry shape lives in `docs/registry/lanes.schema.json` with the lane
registry it validates.
