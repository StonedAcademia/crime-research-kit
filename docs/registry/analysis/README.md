# Analysis Registry Shards

Neutral default vocabulary and scoring tables for analysis report classifiers.

Case-specific terms belong in a case `analysis_vocabulary.json` override, not in
these default shards or in `src/`.
