# Test fixtures

Shared, committed input files for the test suite. Grouped by kind:

- `docs/` — small authored documents (e.g. `sample_report.pdf`) used by the
  Docling parsing and OCRmyPDF smoke tests.

Fixtures here are synthetic test data, not real evidence.
